.row.wrapper.border-bottom.white-bg.page-heading
  .col-lg-8
    h2 分享点击日志
    ol.breadcrumb
      li= link_to '首页', admin_root_path
      li.active 列表

.wrapper.wrapper-content.animated.fadeInRight
  .ibox.float-e-margins
    .ibox-content
      = form_tag params.permit(:user_id), method: :get, class: 'form-inline order-filter-form m-b' do
        - if @user.blank?
          .form-group.m-r
            = label_tag :shared_user_name, ViewLog.human_attribute_name(:shared_user_id)
            | &nbsp;&nbsp;
            = text_field_tag :shared_user_name, @shared_user_name, class: 'form-control'
        .form-group.m-r
          = label_tag :viewer_name, ViewLog.human_attribute_name(:viewer_id)
          | &nbsp;&nbsp;
          = text_field_tag :viewer_name, @viewer_name, class: 'form-control'
        .form-group.m-r
          = label_tag :uri, ViewLog.human_attribute_name(:uri)
          | &nbsp;&nbsp;
          = text_field_tag :uri, @uri, class: 'form-control'
          
        = submit_tag '搜索', class: 'btn btn-success'

      = render 'list'